@model EmailOnlyViewModel
@{
    Layout = "_AuthLayout";
}

<h2 class="font-display text-2xl font-bold">Email sign-in link</h2>
<p class="text-sm text-muted-foreground mt-1 mb-6">Enter your email and we'll send you a one-time sign-in link — no password needed.</p>

<form asp-controller="Account" asp-action="MagicLink" method="post" class="space-y-4">
    <div>
        <label asp-for="Email" class="label">Email</label>
        <input asp-for="Email" class="input" autocomplete="email" placeholder="you@example.com" />
        <span asp-validation-for="Email" class="text-danger text-xs"></span>
    </div>

    <button type="submit" class="btn btn-primary w-full">Email me a link</button>
</form>

<p class="text-center text-sm text-muted-foreground mt-6">
    <a class="text-primary hover:underline" asp-controller="Account" asp-action="Login">Back to sign in</a>
</p>
